<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="CodexBarWin.Controls.ProviderCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CodexBarWin.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid
        Padding="12,10"
        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="1"
        CornerRadius="6"
        PointerPressed="OnCardClicked">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Content Area (3 Columns) -->
        </Grid.RowDefinitions>

        <!-- Header: Icon, Name, Status indicator -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Viewbox Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center">
                    <PathIcon x:Name="ProviderIcon" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                </Viewbox>
                <TextBlock
                    x:Name="ProviderName"
                    FontWeight="SemiBold"
                    FontSize="13"
                    VerticalAlignment="Center"/>
            </StackPanel>

            <Grid Grid.Column="2" VerticalAlignment="Center">
                <Ellipse x:Name="StatusNormal" Width="8" Height="8" Fill="#10B981" Visibility="Collapsed"/>
                <ProgressRing x:Name="StatusLoading" Width="14" Height="14" IsActive="True" Visibility="Collapsed"/>
                <Ellipse x:Name="StatusError" Width="8" Height="8" Fill="#F59E0B" Visibility="Collapsed"/>
            </Grid>
        </Grid>

        <!-- Content Area: 3 Columns Grid -->
        <Grid Grid.Row="1" x:Name="ContentGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="Col0" Width="*"/>
                <ColumnDefinition x:Name="Col1" Width="*"/>
                <ColumnDefinition x:Name="Col2" Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Column 0: Session (5h) -->
            <StackPanel Grid.Column="0" x:Name="SessionSection" Spacing="4" Margin="0,0,6,0">
                <TextBlock
                    x:Name="SessionLabelText"
                    Text="Session"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    HorizontalAlignment="Left"/>
                <local:UsageMeter
                    x:Name="SessionMeter"
                    Height="6"
                    VerticalAlignment="Center"/>
                <TextBlock
                    x:Name="SessionPercentText"
                    FontSize="10"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    HorizontalAlignment="Left"/>
            </StackPanel>

            <!-- Column 1: Weekly -->
            <StackPanel Grid.Column="1" x:Name="WeeklySection" Spacing="4" Margin="6,0,6,0" Visibility="Collapsed">
                <TextBlock
                    x:Name="WeeklyLabelText"
                    Text="Weekly"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    HorizontalAlignment="Left"/>
                <local:UsageMeter
                    x:Name="WeeklyMeter"
                    Height="6"
                    VerticalAlignment="Center"/>
                <TextBlock
                    x:Name="WeeklyPercentText"
                    FontSize="10"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    HorizontalAlignment="Left"/>
            </StackPanel>

            <!-- Column 2: Tertiary (Monthly Total) -->
            <StackPanel Grid.Column="2" x:Name="TertiarySection" Spacing="4" Margin="6,0,0,0" Visibility="Collapsed">
                <TextBlock
                    x:Name="TertiaryLabelText"
                    Text="Total"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    HorizontalAlignment="Left"/>
                <local:UsageMeter
                    x:Name="TertiaryMeter"
                    Height="6"
                    VerticalAlignment="Center"/>
                <TextBlock
                    x:Name="TertiaryPercentText"
                    FontSize="10"
                    Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                    HorizontalAlignment="Left"/>
            </StackPanel>
        </Grid>

        <!-- Error message overlay -->
        <Grid Grid.Row="1" x:Name="ErrorPanel" Visibility="Collapsed" MinHeight="40">
            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE7BA;" FontSize="12" Foreground="{ThemeResource SystemFillColorCautionBrush}"/>
                <TextBlock
                    x:Name="ErrorText"
                    Text="Failed to load"
                    TextWrapping="Wrap"
                    MaxWidth="200"
                    Style="{StaticResource CaptionTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <HyperlinkButton
                    x:Name="ResetCookieButton"
                    Content="Configure"
                    Click="OnResetCookieClicked"
                    Padding="4,2"
                    FontSize="11"
                    Visibility="Collapsed"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
